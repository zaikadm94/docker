version: '3.8'
services:
  db:
    image: postgres:12-alpine
    ports:
      - '3300:5432'
    volumes:
      - ./data:/var/lib/postgres
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: 9mRE
